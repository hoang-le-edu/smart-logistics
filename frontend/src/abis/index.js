// Placeholder ABIs - will be replaced after contract compilation
// Run: npx hardhat compile && npx hardhat run scripts/deploy.js

export const LogiTokenABI = {
  address: "",
  abi: [],
};

export const ShipmentRegistryABI = {
  address: "",
  abi: [],
};

export const EscrowMilestoneABI = {
  address: "",
  abi: [],
};

// Note: After deploying contracts, copy the generated JSON files from:
// ../deployments/{network}.json or artifacts/contracts/
